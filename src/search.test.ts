import{describe,it,expect}from"vitest";import{searchTracks,Track}from"./search";const songs:Track[]=[{id:"1",title:"Isabella",artist:"Search",path:"C:/Music/Isabella.mp3"},{id:"2",title:"Fantasia Bulan Madu",artist:"Search",path:"C:/Music/Fantasia.mp3"}];describe("searchTracks",()=>{it("finds title",()=>expect(searchTracks(songs,"isabella")[0].id).toBe("1"));it("tolerates typo",()=>expect(searchTracks(songs,"isabela")[0].id).toBe("1"));it("returns library for blank query",()=>expect(searchTracks(songs,"")).toHaveLength(2))});